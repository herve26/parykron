import React from 'react'
import styled from 'styled-components';

const SectionWrapper = styled.div`
    margin-left: 48px;
    margin-top: 24px;
`

export default function VisitedSection(){
    return(
        <SectionWrapper>
            <div>Visited Section</div>
        </SectionWrapper>
    )
}

// export VisitedSection