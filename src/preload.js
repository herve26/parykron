// const { startServer } = require('./server');

window.electron = require('electron');
// console.log(require('electron').remote)
// window.serverPort = (async () => await startServer(require('electron').remote.app))()

// require = require('electron').remote.require
// console.log('hello')